import { Child } from '../Child'

/* ========================================================================
                                Tests   
======================================================================== */

export const Parent = () => {
  return (
    <div>
      <div>This is the parent.</div>

      <Child />
    </div>
  )
}
